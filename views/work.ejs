<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="post.css">
    <title>Blog</title>
</head>
<body>
    <%var da=new Date(result.d);%>
    <%var year=da.getFullYear();%>
    <%var month=da.getMonth();%>
    <%var date=da.getDate();%>
    <%var fd = `${date}-${month+1}-${year}`;%>
    <%if((result.img)!=null){%>
    <%var image=(result.img).toString('base64') %>
    <div id="pic" style="background-image: url('data:image/*;base64,<%=image%>');"></div>
    <%}else{%>
    <div id="pic" style="background-color: rgb(197, 194, 194);"></div>
    <%}%>
    

    </div>
    <div class="blog">
            <h1 class="title" placeholder="Blog Title.." ><%=result.topic%></h1>
            <p class="date"><%=fd%></p>
            <p class="title" id="b"><%=result.blog%></p>
    </div>
</body>
</html>